@model IEnumerable<MusicApp.Models.MediaTypeBaseViewModel>

@{
    ViewBag.Title = "Media Type List";
}

<h2>@ViewBag.Title</h2>
<hr />

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
        </tr>
    }

</table>
